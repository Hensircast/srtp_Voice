# 基础运行 console 模式不需要第三方库。
# mic/vad 录音和播放需要：
sounddevice>=0.4.6
soundfile>=0.12.1

# LLM 使用 OpenAI-compatible API 时需要：
requests>=2.31.0
python-dotenv>=1.0.0

# 串口输出到 Arduino/STM32/ESP32 时需要：
pyserial>=3.5

# 临时联网 TTS 演示：
edge-tts>=6.1.10

# 测试时需要安装
pytest>=9

# 真实模型接入时按需安装：
# onnxruntime
# onnxruntime-gpu
# funasr
# modelscope
# torch
