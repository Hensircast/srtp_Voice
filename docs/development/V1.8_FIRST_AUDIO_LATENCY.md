# V1.8 首响延迟优化（2026-09-18）

## 依据和边界

- 原始收口代码 `71ee5f7`，对应 main squash `d57f5ba`。
- 本地已有 11 轮指标中，ASR final（VAD 结束后）p50 为 2391 ms，LLM 首 token p50 为 1282 ms，TTS 首块 p50 为 610 ms。各阶段中位数不能直接相加当作总耗时。
- 原有 `time_to_playback_ms` 从 turn 创建开始，包含等待用户、说话、VAD 静音确认，不能当作说完话后的纯响应时间。
- 代码会在检测到端点或录音上限的同一帧先调度 partial，再等待它结束并执行 final。这会在触发时额外增加一次 ASR 解码。
- 修复将两种端点检查移到 partial 调度之前。已在执行的 partial 仍安全等待；final 仍独立识别完整音频，且只发送一次。
- 没有更换模型、减少识别精度、改变 VAD 静音阈值或修改用户环境。

## 验证

- 新增 VAD 正常结束、录音达到上限两项确定性回归：修复前均失败，修复后通过。
- 完整离线测试：375 passed；compileall、pip check、git diff --check 通过。
- 真实麦克风首响改善幅度尚未测量；不能把省下一次潜在调用宣称为固定秒数提升。
- CI 和 PR 链接以本次交付报告为准。

## PowerShell 复测

在仓库根目录打开一个新的 PowerShell，使用耳机。以下变量仅用于该终端；关闭终端即可恢复，不覆盖既有测试输出和 memory。

```powershell
$runName = 'first-audio-' + (Get-Date -Format 'yyyyMMdd-HHmmss')
$env:OUTPUT_DIR = "outputs/$runName"
$env:MEMORY_FILE = "$env:OUTPUT_DIR/memory.json"
.\venv\Scripts\python.exe main.py --mode vad --streaming --continuous
```

连续说 5–10 轮自然问句，第一轮与后续热启动分开比较，保持模型和 VAD 配置不变。按 Ctrl+C 后，在同一终端运行：

```powershell
$m = Get-Content -Raw -Encoding UTF8 "$env:OUTPUT_DIR/streaming_metrics.json" | ConvertFrom-Json
$m.summary | ConvertTo-Json -Depth 6
```

重点报告：

- `endpoint_to_playback_ms`：VAD 确认结束到播放启动，不包含此前的 VAD 静音确认；仍是软件事件计时，不是声卡实际出声测量。
- `endpoint_to_first_audio_ms`：VAD 确认结束到音频就绪。
- `asr_final_ms`：端点后的识别等待与 final 解码。
- `post_asr_setup_ms`：final 识别后到请求 LLM（含 SER、情绪状态、memory 等准备）。
- `llm_first_token_ms`、`first_sentence_ms`、`tts_first_chunk_ms`：后续各阶段。

验收还需确认 partial 正常显示、final 无丢字、句子播放顺序正确、长句和短句均能继续多轮。
